import dotenv from "dotenv";
dotenv.config();

async function main(): Promise<void> {
    // TODO: Do bunch of tests here
}

main().catch((error) => {
    console.error(error);
    process.exitCode = 1;
});
